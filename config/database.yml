#
# we are using the mbms database 
#

development:
  adapter: postgresql
  #database: mbms_testing
  database: mbms
  #username: bones
  username: postgres
  password: 
  encoding: utf8
  host: localhost

production:
  adapter: postgresql
  database: mbms
  #username: bones
  username: postgres
  password: 
  encoding: utf8
  host: localhost

# Warning: The database defined as 'test' will be erased and
# re-generated from your development database when you run 'rake'.
# Do not set this db to the same as development or production.
test:
  adapter: mysql
  database: my.maxnet.ao_test
  username: root
  password:
  host: localhost

